spring:
  application:
    name: ai

  ai:
    google:
      genai:
        api-key: ${GOOGLE_API_KEY}
        chat:
          model: gemini-2.0-flash-lite
          enable-cached-content: false

# Futuro: Postgres + pgvector + embeddings para RAG
#  datasource:
#    url: jdbc:postgresql://localhost:5432/ai
#    username: ai
#    password: ai
#  ai:
#    google:
#      genai:
#        embedding:
#          api-key: ${GOOGLE_API_KEY}
#    vectorstore:
#      pgvector:
#        initialize-schema: true
